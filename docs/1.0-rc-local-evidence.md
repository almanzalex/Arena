# RLX 1.0.0rc1 local evidence

Date: 2026-07-23

Branch: `codex/1.0`

Verdict: local implementation proof passes; final `v1.0.0` release proof is
not yet complete.

This record is deliberately not an `rlx.release-evidence/v1` index. A real
release index must be produced outside the source commit, content-bind the exact
wheel/sdist and R-01…R-14 records, use a release-manager key delivered through
an independent trust channel, and pass `rlx release verify`.

## Automated proof run

| Proof | Result |
|---|---|
| Fast unit/acceptance/adversarial suite | 282 passed, 3 slow/Docker deselected in 64.18 s |
| Current-tree hermetic gates | Fresh venv plus Docker `--network none`: 3 passed, 282 deselected in 205.00 s |
| Ruff, release-truth drift, diff whitespace | Pass |
| Wheel/sdist build | `rlx-1.0.0rc1-py3-none-any.whl` and `rlx-1.0.0rc1.tar.gz` built from an isolated PEP 517 environment |
| Core-only clean install | Version, doctor, and packaged 22-entry schema registry pass outside the checkout |
| Quickstart clean install | Doctor ready; source-free handoff passes twice at the same output path with identical identities |
| Out-of-tree plugin wheel | Lazy load, use, uninstall, and post-uninstall core doctor pass |
| Dependency audit | Exact core and quickstart environments: zero known vulnerabilities; only unpublished local `rlx` is explicitly skipped |
| Static security scan | Bandit medium/high: zero unresolved results after four reviewed trust-boundary annotations |
| Secret scan | Zero findings outside excluded test/docs/example fixtures and binary artifacts |

The release workflow regenerates a reproducible CycloneDX JSON SBOM, dependency
audit, Bandit report, secret scan, and SHA-256 checksum file from the exact
quickstart environment. It then asks GitHub `actions/attest@v4` for both SLSA
build provenance and an SBOM attestation over the wheel and sdist. Local runs
cannot substitute for those repository-bound Sigstore attestations.

## Executed value flows

### Atomic producer-to-consumer handoff

An installed wheel exported a policy, ran 11 source-conformance cases, pushed
and pulled it through `file://`, and reran safely over the same destination:

- source/restored policy:
  `sha256:15bb6b295e48b20ff2cb11e42010eb13ae7d11abc283fd730361819337a11412`
- evaluation intent:
  `sha256:5577fd6dfe79a575eee66c7e40cf7e1d6398ca7df84d8ecbae846bdf7c423991`
- every pulled blob, the artifact identity, and the source-conformance suite
  verified on both runs.

### Native, OpenEnv, and Gimitest bindings

`examples/1.0/run_local_boundaries.py` launched a separate loopback OpenEnv
HTTP process, repinned its schema, proved trace equivalence, and executed every
evaluation cell in a supervised process. Gimitest ran from a newly resolved
venv rather than the parent interpreter; doctor verified RLX `1.0.0rc1`,
Gimitest `1.0`, Torch `2.13.0`, and PettingZoo `1.26.1` in that worker:

- shared task intent:
  `sha256:bf82666ed34036858261638face2a0fafd4cea46724bd82b4551b01d0d446897`
- shared evaluation intent:
  `sha256:b2c26a91a82029e71f7798a25e3401b291d3c26dcf15a424fcbeb9258d0dfecc`
- shared semantic result:
  `sha256:3735a0cead9ea43ba9fc4e599d9742712894b7f3f7a28e724670a651059c714d`
- distinct native/OpenEnv/Gimitest binding digests;
- denominators: 2 attempted, 2 completed, 0 failed;
- non-no-op reward-transform result:
  `sha256:aad6aca40eb0857764bf59fca8a172b6cbd05c4dbfcf7a236dbb12a7c4127c4d`,
  with distinct evaluation intent
  `sha256:5edeac19940d00f879f8e3d0782be9faef7802643432a416ad137d1297b52566`,
  proving a semantic intervention changes both intent and result identity.

This is strong local contract proof. It does not claim a separately operated
production OpenEnv service or release-commit CI evidence for the isolated
Gimitest environment.

### 0.2 compatibility flow

`bash examples/eval/run_demo.sh` passed from the checked-in historical demo:

- preflight and executed evaluation intent:
  `sha256:c5693a311acfd73882841b67785a7646171ec6091e28a621438b17fd7163dca0`
- state/denominators: complete, 9 attempted, 9 completed, 0 failed;
- non-transitive cyclic payoff retained with `ranking: null`;
- loss dataset slice and locked eval bundle published successfully.

### Public CleanRL producer

The producer rehearsal pinned official CleanRL commit
`fe8d8a03c41a7ef5b523e2e354bd01c363e786bb`, trained CartPole DQN for 4,000
steps, and exported the exact upstream layer layout through BYO TorchScript.
The trainer saved the checkpoint and later exited nonzero because optional
`moviepy` was absent; RLX consumed the complete checkpoint without erasing that
lineage. A consumer loaded policy
`sha256:e96ee4fe2eb63cb5de3dbad3c2a042c88ca8a97f8630daa753081f534f6c7424`
and four source-conformance cases without CleanRL or the exporter bridge.

## Final-release gates still open

| Gate | Current evidence | What must happen before `v1.0.0` |
|---|---|---|
| R-01 platform CI | Four Python/platform rows configured | Run green on the exact remote commit: Linux x86_64 and macOS arm64, Python 3.12/3.13 |
| R-02 hermetic handoff | Local venv and Docker network-none pass | Repeat against the single final wheel/sdist in release CI |
| R-03 independent handoff | Author-operated flows pass | Two non-authors on separate Linux/macOS machines complete timed, failure-injected journeys |
| R-04 stable stores | File stable; HF contract/simulation pass | Live immutable Hugging Face push/pull with release evidence |
| R-05 OpenEnv | Separate local HTTP process passes | Fresh qualification against a separately deployed/operated service |
| R-06 Gimitest | Fresh resolver-isolated interpreter, strict worker, and non-no-op semantics pass locally | Repeat from the exact artifact in claimed-platform release CI and bind the report |
| R-07 adversarial/soak | Broad reduced corpus passes | Full release-scale soak/resource envelope and mutation checks |
| R-08 supply chain | Local SBOM/audit/static/secret scans pass; workflow configured | Remote provenance/SBOM attestations and release security review |
| R-09 compatibility | 0.2 demo and legacy suite pass | Bind authentic 0.2/0.3/0.5 fixture reports to the release index |
| R-10 performance | Functional budgets pass | Linux/macOS repeated baseline report within declared thresholds |
| R-11 public distribution | Local exact wheel install passes | TestPyPI/PyPI Trusted Publishing and clean public-index install |
| R-12 recovery | Transactions, corruption, timeout, and process-group tests pass | Release rollback rehearsal and full fault matrix |
| R-13 docs truth | Single-source drift checker passes | Repeat on the final tag and independently follow the published docs |
| R-14 evidence integrity | Signed-index/current-ledger tests pass | Assemble and sign all real gate files; verify exact tag/assets with independent keys |

The RC must also bake for seven days without artifact changes. No code path
silently converts these missing external records into a passing release claim.
