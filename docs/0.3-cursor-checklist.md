# Completed plan — Arena 0.3 Stage 2

Use with [0.3-delivery.md](0.3-delivery.md). Do **not** reopen 0.1/0.2 BYO work except maintenance. Do **not** implement `arena train` (0.4). Do **not** implement dynamic agents unless RFC 005 is explicitly un-parked for a named env.

## Phase checklist (copy into todos)

1. [x] Phase 0: tag v0.2.0; optional human eval sign-off; branch release/0.3
2. [x] Phase 1: eval_provider registry + task CLI; 0.2 suite stays green
3. [x] Phase 2: OpenEnv packager + verify-equivalence + qualify (T-01…T-03)
4. [x] Phase 3: OpenSpiel `tic_tac_toe` + qualify
5. [x] Phase 4: Gimitest provider + lineage (I-01); native default unchanged
6. [x] Phase 5: store adapters file:// then HF; push/pull --verify (I-02); I-03 offline
7. [x] Phase 6: S-01 budgets; U-03 docs; RFC 000 → 0.3 Done; tag v0.3.0

## Done when

Roadmap exit: same policy/eval through native + external task runtime with verified semantics; external test/storage tools attach without changing artifact identity.
