# RLX 0.4 implementation evidence

Recorded 2026-07-22 on Python 3.12, branch `codex/0.4-boundaries`.
No release tag or remote push was created.

## Automated gates

```text
python3 -m ruff check .
  All checks passed.

python3 -m pytest -q
  233 passed, 3 deselected

python3 -m pytest -q -m slow
  3 passed, 233 deselected

python3 -m build --outdir /tmp/rlx-0.4-build-20260722
  rlx-0.4.0.tar.gz and rlx-0.4.0-py3-none-any.whl built successfully
```

The wheel contains `rlx/runtime/training.py`,
`rlx/runtime/dynamic_aec_match.py`, and the expanded mirror implementation.
It was installed into a separate venv and imported from
`.../site-packages/rlx/__init__.py` with the repository absent from `sys.path`;
the complete boundary demo then passed against that installed wheel.

## Composed user-flow simulation

Command:

```bash
bash examples/boundaries/run_demo.sh
```

Observed result:

- Dynamic AEC: 1/1 episode complete, zero failures, four join/leave events.
- Dynamic task qualification: immutable contract and frozen semantic trace passed
  through `rlx adapter qualify`; trace digest
  `sha256:3a80f8fa6eb97433c49cb04fdff8f7f4c76037167186db109df6cf1dbc11c845`.
- Teacher collection: 3/3 episodes complete; three episodes materialized.
- Behavior cloning: nine examples; loss `1.1398902 → 0.00010327`.
- Trained policy: source-conformance passed; fresh match completed with zero failures.
- Connect Four: match completed and authoritative frozen trace matched
  `sha256:b3aa7b18ddf57c455950022bb17879604ac9c8da97be456553b40f8bf2e4012f`.
- HF/OCI/W&B/MLflow simulations: verified push and pull; all restored the exact
  trained policy identity
  `sha256:a39fc97d18e96e15f406ed3b1f4573bfcde03b6a1c4943e8bc0f2f7a3806d394`.

## Honest remote boundary

No provider credentials were available, so no live HF/OCI/W&B/MLflow mutation was
performed. `examples/boundaries/live_store_smoke.py` is the opt-in authenticated
gate and refuses simulation URIs. Live call shapes were checked against the official
ORAS, W&B, and MLflow artifact APIs; simulation evidence is labeled separately.
