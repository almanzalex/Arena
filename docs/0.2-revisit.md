# Later / revisit (explicitly not required for RLX 0.2 Done)

These strengthen a *maximal* interoperability claim but are **out of the locked 0.2
scope**. Attack them when opening 0.3+ or a dedicated validation epic.

| Item | Status | Why deferred |
|------|--------|----------------|
| External MARL matrix (BenchMARL, etc.) | Revisit | Upstream/env stack failures are not RLX product bugs; pilot RPS + CleanRL lesson already inform 0.1/0.2 |
| `rlx train` / training recipes | Non-goal | Product boundary (RFC 000) |
| OpenEnv / OpenSpiel / Gimitest | Non-goal | 0.3+ |
| External / hosted stores | Non-goal | Local-first `.rlx/` only in 0.2 |
| Silent Elo / universal ranking | Non-goal | MET-03 keeps matrices primary |
| Dynamic agent birth/removal lifecycle | Parked ([RFC 005](../rfcs/005-dynamic-agents.md)) | Fail-loud in 0.2.0; revive only if a 0.3 pilot env requires it |
| Multi-OS human sign-off diversity | Ceremony | Template in `docs/eval-usability-signoff.md`; needs a real second reader |

**0.3 execution plan:** [0.3-delivery.md](0.3-delivery.md) (OpenEnv, OpenSpiel, Gimitest, stores — not training).


## What *is* required for a usable 0.2 claim

1. Checked-in runnable demo: `examples/eval/demo/` + `bash examples/eval/run_demo.sh`
2. Fast suite green: `pytest -q`
3. Hermetic (CI / local): `pytest -m slow -q` (U-01 + eval clean-room)
4. Optional human record: `docs/eval-usability-signoff.md`
