# Later / revisit (explicitly not required for Arena 0.2 Done)

These strengthen a *maximal* interoperability claim but are **out of the locked 0.2
scope**. Attack them when opening 0.3+ or a dedicated validation epic.

| Item | Status | Why deferred |
|------|--------|----------------|
| External MARL matrix (BenchMARL, etc.) | Revisit | Upstream/env stack failures are not Arena product bugs; pilot RPS + CleanRL lesson already inform 0.1/0.2 |
| `arena train` / training recipes | Generalized in 0.5 | Trainer registry, two offline cases, splits, exact resume; see [training.md](training.md) |
| OpenEnv / OpenSpiel / Gimitest | Generalized by qualification in 0.5 | Second OpenEnv contract, three OpenSpiel semantic families, provider process boundary |
| External / hosted stores | Generalized in 0.5 | Shared qualification + user-owned authenticity; no Arena-hosted service |
| Silent Elo / universal ranking | Non-goal | MET-03 keeps matrices primary |
| Dynamic agent birth/removal lifecycle | Generalized in 0.5 ([RFC 005](../rfcs/005-dynamic-agents.md)) | Exact/role resolvers and re-entry history; not silent lifecycle inference |
| Multi-OS human sign-off diversity | Ceremony | Template in `docs/eval-usability-signoff.md`; needs a real second reader |

**0.3 completion:** [0.3-complete.md](0.3-complete.md) (OpenEnv, OpenSpiel, Gimitest, stores — not training).


## What *is* required for a usable 0.2 claim

1. Checked-in runnable demo: `examples/eval/demo/` + `bash examples/eval/run_demo.sh`
2. Fast suite green: `pytest -q`
3. Hermetic (CI / local): `pytest -m slow -q` (U-01 + eval clean-room)
4. Optional human record: `docs/eval-usability-signoff.md`
